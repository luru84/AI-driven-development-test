name: CI (placeholder)
on:
  pull_request:
  push:
    branches: [ "main" ]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Select a stack with ./scripts/init-stack.sh <stack> (available: python)"*** End Patchọdọ
