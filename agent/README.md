# Agent workspace

AI 駆動の反復で「状態をファイルに閉じ込める」ための作業場です。基本的な役割は下記のとおりです。

- `plan.md`: 次の一手を箇条書きで管理する計画書。
- `decisions.md`: 仕様解釈やスコープ変更などの判断ログ。
- `prompts/`: LLM に渡す system / user プロンプトの保存場所。
- `context/`: リサーチメモや入力データの断片など、参照用のコンテキスト。

成果物や一時ファイルは `/work` に置き、この `/agent` 配下はできるだけ薄いテキストで保つと回しやすいです。
